|grep
|less
|tee
ps aux|grep
sudo netstat -tulnp;
sudo netstat -tulnp|grep
sudo apt-get install
sudo apt-get update;
sudo apt-get dist-upgrade;
sudo apt-get autoremove;
git clone
ifconfig |grep -A 10
git commit
cd /var/log/
nc -vz #host #port;
